# Search test report — 2026-03-22

7 passed, 2 failed

## Results

PASS Search page loads
PASS Search returns channels and tracks
PASS Search result navigation
PASS Search with no results
FAIL Search clear -- input cleared but results not reset to default state
PASS Search by tag
PASS Search by channel mention
FAIL Track results pagination -- next page button did not change results
PASS Search display modes

## Failure details

### FAIL: Search clear

Input was cleared (searchbox emptied) but the previous "jazz" results remained visible. The "All Channels" / "All Tracks" default links did not reappear. After clicking "Clear":

```
- searchbox "Search radios and tracks" [ref=e13]   ← input empty (correct)
- heading "17 channels" [level=2, ref=e7]           ← old results still showing
- heading "1571 47 track results" [level=2, ref=e9] ← old results still showing
```

Expected: results cleared, back to default state with "All Channels" and "All Tracks" links.
Got: input cleared, but results section unchanged.

### FAIL: Track results pagination

After searching "jazz" (47 track results shown), pagination controls appeared (Previous page disabled, Go to page, Next page). Clicking "Next page" did not change the track list — the same first track remained and "Previous page" stayed disabled.

The heading showed "1571 47 track results" suggesting 1571 total results but only 47 displayed per page — the page index may not be updating on click.

```
- button "Previous page" [disabled, ref=e10]         ← still disabled after Next click
- "Layla Eric Clapton– Orchestral Blues Jazz Version" ← same first track as page 1
```

Expected: second page of track results (different tracks), "Previous page" enabled.
Got: same track list, "Previous page" still disabled.

## Notes

- URL pattern: the app uses `/slug` (e.g. `/radio-tropical`) not `/@slug`. Test 3 still passes as the pattern matches.
- Test 6 (tag search): "#ambient" returned only track results (50), no channel matches — expected behavior.
- Test 7 (mention search): "@oskar" returned 1 channel and 50 track results — working correctly.
- Test 9 (display modes): Display panel showed sort options (Randomize order, Updated, Created, Title, Track count) and a Limit spinbutton — no Grid/List toggle visible in this view, but display controls are present.

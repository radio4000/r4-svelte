# Tooltips

Tooltip positioning and behavior. Tooltips use the Popover API and anchor positioning for placement.

```svelte
<button {@attach tooltip({content: 'Hello world', position: 'top'})}>
	Hey
</button>
```
